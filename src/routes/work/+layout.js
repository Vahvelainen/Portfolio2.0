
//All pages should be able to prerender
export const prerender = true;

//Ssr is not needed with prerendering everytihing
export const ssr = false;

//Client side renderign is acceptable
export const csr = false;
